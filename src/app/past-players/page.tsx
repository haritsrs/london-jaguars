import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { pastPlayers } from "@/data/pastPlayers";

export default function PastPlayersPage() {
  return <div className="archive-page"><section className="archive-hero archive-hero--light"><div className="site-frame"><p className="eyebrow">Club archive · 02</p><h1>Past<br /><em>players.</em></h1><p className="archive-hero__intro">A record of the players attached to the Jaguars story across the canonical club ledger. The archive keeps the names visible; the football remains the point.</p></div></section><section className="site-frame archive-list archive-list--players"><div className="section-header"><div><p className="eyebrow">The record</p><h2>Those who<br />came before.</h2></div><p className="archive-list__aside">{pastPlayers.length} names in the published historical transfer record.</p></div><div className="past-player-ledger">{pastPlayers.map((player) => <article key={`${player.player}-${player.date}`}><div><span className="eyebrow">{player.date}</span><h3>{player.player}</h3></div><div><span>{player.otherTeam}</span><small>Recorded club movement · {player.feeM ? `£${player.feeM}m` : "fee undisclosed"}</small></div></article>)}</div><p className="archive-footnote">The source workbook notes that the duplicated incoming/outgoing sheets do not establish reliable direction. Entries are therefore shown as published historical records, not as a definitive classification of arrivals or departures.</p></section><section className="archive-link-band archive-link-band--light"><div className="site-frame"><Link href="/legends">Club legends <ArrowUpRight size={18} /></Link><Link href="/staff">Coaching staff <ArrowUpRight size={18} /></Link></div></section></div>;
}
